---
engines:
  fixme:
    enabled: true
  govet:
    enabled: true
  golint:
    checks:
      enabled: true
      GoLint/Comments/DocComments:
        enabled: false
      GoLint/Naming/MixedCaps:
        enabled: false

ratings:
  paths:
    - "**.go"

exclude_paths:
  - test/
