---
engines:
  fixme:
    enabled: true
  shellcheck:
    enabled: true
  govet:
    enabled: true
  golint:
    enabled: true
    checks:
      GoLint/Comments/DocComments:
        enabled: false
      GoLint/Naming/MixedCaps:
        enabled: false
  shellcheck:
    enabled: true

ratings:
  paths:
    - "**.go"
    - "**.bash"

exclude_paths:
  - test/
