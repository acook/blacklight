;; A quick file read example

newq                  ;; creates a new Q that we'll use as our read-queue
'examples/_text.txt'  ;; this is the filename we'll read from
read                  ;; starts reading from our file in a background thread
swap                  ;; hide the fd T and being read-queue to top
q-to-cv               ;; blocks until EOF and returns CV
print                 ;; prints CV to stdout
