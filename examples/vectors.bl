;; vectors are typed sequences of items
;; they allow random-access to items in the middle of the sequence
;; the ato and rmo operations are 0-based offsets

(1 2 3 4 5)

;; ato retuns an item at offset
1 ato print

;; rmo removes an item at offset
2 rmo

;; len returns the number of items in V
len print

;; finally print the V
print
