#!/usr/bin/env bash

source "$(dirname ${BASH_SOURCE[0]})/lib/_shared.bash"
include "bl_env"

safe_cd $BL_ROOT_PATH
say "doing build/test..."
load ./build/test
safe_cd $BL_ROOT_PATH
say "doing build/run..."
load ./build/run

if scriptsame; then
  ok "CI validation completed successfully!"
fi
