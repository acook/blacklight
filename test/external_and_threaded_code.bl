'examples/hello_world.bl' co  ;; run file in a new thread with its own $
'examples/basic_math.bl'  co  ;; run file in this thread with our $

;;newq 1 enq 2 enq 4 enq    ;; make a Q of work
;;.
;;  ..
;;    print                 ;; print items in Q
;;  ..  proq                ;; interate over items in Q until empty
;;. bkg                     ;; run this WV in the background taking the top item with it

;;newq newq                 ;; create recieve and send queues (swapped for work thread)
;;.
;;  ..
;;    deq                   ;; will block if the queue is empty
;;    n-to-s rot swap enq   ;; convert number into a cv (string) and send back
;;    swap                  ;; reorder queues so we can loop without confusion
;;  ..  loop                ;; using loop since it goes forever
;;. work
;;1 enq 2 enq 3 enq 4 enq   ;; send some numbers to be converted
;;swap q-to-v print         ;; get contents of receive q and print them
